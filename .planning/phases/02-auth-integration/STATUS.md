# Phase 2 Status

Status: not started

Current state:
- The app still needs the session model swapped from local state to Supabase Auth.

Next action:
- Implement `useAuth` and update the existing auth entry points before moving to the task data layer.
